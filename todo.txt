ecdb check if component is already initialized
move network packet buffer out of ECDB?
start of game it shows ghost in the top left corner becuase we don't have server info - instead maybe just don't show a ghost
fix: random server crash on join after player requested
fix chat box UI when chat message is really long
fix chat snapshot buffer being hardcoded
ecdb in built hierarchy?
ecdb integrate paged memory approach to make low use components make more sense
some way to roll back not entire state but a subset of state, without losing subsequent state (i.e particles, camera)
use latest input to determine direction by using input event order
delay other clients state similar to position
track frames rather than time to ensure we don't have to send the full timestamp with each update